#! /bin/sh
#
# This file contains configuration information for mpicxx.  This is
# essentially just the variable-initialization part of mpicxx.
# --------------------------------------------------------------------------
# Set the default values of all variables.
#
# Directory locations: Fixed for any MPI implementation.
# Set from the directory arguments to configure (e.g., --prefix=/usr/local)
prefix=/home/kaitykoo122/wrf_dependencies/mpich
exec_prefix=${prefix}
sysconfdir=${prefix}/etc
includedir=${prefix}/include
libdir=${exec_prefix}/lib

# Default settings for compiler, flags, and libraries.
# Determined by a combination of environment variables and tests within
# configure (e.g., determining whehter -lsocket is needee)
CXX="g++"
CXXFLAGS=" "
LDFLAGS=" -L/home/kaitykoo122/wrf_dependencies/netcdf/lib -L/home/kaitykoo122/wrf_dependencies/grib2/lib "
LIBS="-lopa -lmpl  -lpthread -lnetcdf -lz "
MPICXXLIBNAME="mpichcxx"
MPICH_VERSION="3.0.4"
